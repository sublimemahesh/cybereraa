!(function (o) {
  "use strict";
  (o.fn.serialscrolling = function (t) {
    const l = o.extend({}, o.fn.serialscrolling.defaults, t),
      e = o(l.targetSelector),
      n = o(window);
    var i = o(this),
      s = this;
    let a = !1;
    return (
      o.extend(this, {
        init: function () {
          n
            .off("scroll.serialscrolling")
            .on("scroll.serialscrolling", this.handleCurrent)
            .trigger("scroll"),
            this.off("click.serialscrolling").on(
              "click.serialscrolling",
              this.handleEvent
            );
        },
        handleCurrent: function () {
          let r;
          e.each(function () {
            const t = o(this);
            t.offset().top - n.height() / 2 < n.scrollTop() &&
              (r = l.getTrigger(t, i));
          }),
            r && !r.is(a) && (s.setCurrent(r), l.callback && l.callback(id));
        },
        handleEvent: function () {
          var t = o(this),
            t = l.getTarget(t);
          return s.scrollTo(t, l.duration, l.easing), !1;
        },
        setCurrent: function (t) {
          a && a.removeClass("is-current"), (a = t).addClass("is-current");
        },
        scrollTo: function (t, r, e) {
          o("body, html").animate(
            { scrollTop: t.offset().top + l.offsetTop },
            r,
            e
          );
        },
      }),
      this.init(),
      this
    );
  }),
    (o.fn.serialscrolling.defaults = {
      targetSelector: "[data-serialscrolling-target]",
      getTarget: function (t) {
        t = t.attr("data-serialscrolling");
        return o('[data-serialscrolling-target="' + t + '"]');
      },
      getTrigger: function (t, r) {
        t = t.attr("data-serialscrolling-target");
        return r.filter('[data-serialscrolling="' + t + '"]');
      },
      duration: 800,
      easing: "easeInOutExpo",
      offsetTop: 0,
      callback: !1,
    });
})(jQuery),
  ($.easing.easeInOutExpo = function (t, r, e, l, n) {
    return 0 == r
      ? e
      : r == n
      ? e + l
      : (r /= n / 2) < 1
      ? (l / 2) * Math.pow(2, 10 * (r - 1)) + e
      : (l / 2) * (2 - Math.pow(2, -10 * --r)) + e;
  });
